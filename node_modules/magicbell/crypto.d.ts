type UserData = {
    userExternalId?: string;
    userEmail: string;
} | {
    userExternalId: string;
    userEmail?: string;
} | {
    id: string;
} | {
    _id: string;
} | {
    email: string;
};
export declare function createHmac(secret: string, data: UserData): string;
export declare function createHmac(secret: string, data: string): string;
export {};
