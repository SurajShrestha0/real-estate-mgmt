import { ClientOptions, FeatureFlag, RequestArgs, RequestOptions } from './types';
export declare const DEFAULT_OPTIONS: Partial<ClientOptions>;
export declare class Client {
    #private;
    constructor(options: ClientOptions);
    hasFlag(flag: FeatureFlag): never;
    request<TResponse = any>({ method, path, data, params, headers: reqHeaders }: RequestArgs, options?: RequestOptions): Promise<TResponse>;
}
